#include<stdio.h>
#include<conio.h>
 int main()
  {
  	int x ; 
  	 printf("enter the value");
  	 scanf("%d",x);
  	 
  	 if(x/2==0);
if(a/2==0);
{
  pintf("it is even no");
}
else
{
pintf("odd");
}

printf("the integer is %d",x);
 return 0;
 
